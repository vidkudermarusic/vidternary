% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_plot_data_prep.R
\name{compute_point_styling}
\alias{compute_point_styling}
\title{Work out each point's plotted size, color, and shape}
\usage{
compute_point_styling(
  ternary_points1,
  matrika,
  optional_param1,
  optional_param1_representation,
  optional_param2,
  color_palette,
  use_manual_point_size,
  manual_point_size,
  is_categorical_group,
  selected_groups
)
}
\arguments{
\item{ternary_points1}{Data frame of computed ternary coordinates (\code{A},
\code{B}, \code{C} columns), one row per plotted point so far.}

\item{matrika}{The prepared data frame \code{optional_param1}/\code{optional_param2}
values are read from (element columns numeric, optional-parameter
columns preserved as-is for categorical grouping).}

\item{optional_param1}{Optional \verb{list(col = <column name(s)>, filter = ...)} driving point size/type; \code{NULL} for the plain default styling.}

\item{optional_param1_representation}{\code{"point_size"} or \code{"point_type"} -
how \code{optional_param1} maps onto the plotted points.}

\item{optional_param2}{Optional \verb{list(col = <column name>, filter = ...)}
driving point color / categorical grouping; \code{NULL} for plain black
points.}

\item{color_palette}{Palette name (\code{"blue"}, \code{"red"}, \code{"viridis"}, or
\code{"rainbow"}) used when \code{optional_param2} is numeric rather than
categorical.}

\item{use_manual_point_size}{If \code{TRUE}, use \code{manual_point_size} for every
point instead of \code{optional_param1_representation}'s mapping.}

\item{manual_point_size}{Fixed point size used when
\code{use_manual_point_size} is \code{TRUE}.}

\item{is_categorical_group}{Whether \code{optional_param2}'s column has been
detected as categorical (drives group-based filtering/coloring instead
of a continuous color scale).}

\item{selected_groups}{Character vector of categorical group values (from
\code{optional_param2}) to include, when \code{is_categorical_group} is \code{TRUE};
other rows are excluded. Reassigned to "every group present" if none of
the originally-requested groups actually match any data.}
}
\value{
This function's entire local environment as a list
(\code{as.list(environment())}) - \code{pointSize}, \code{pointType}, \code{pointCol},
\code{ternary_points1} (possibly row-filtered), \code{selected_groups} (possibly
reassigned), \code{param1_values}, \code{param1_bins}, \code{unique_groups},
\code{group_colors}, \code{group_counts}, \code{MIN_POINT_SIZE}, and \code{MAX_POINT_SIZE}
are the fields \code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} and the preview/save
renderers actually read back; the rest are this block's own internal
working variables, echoed back unchanged from how they already existed
in \code{prepare_ternary_plot_data()}'s own environment before this
extraction.
}
\description{
Turns Optional Parameter 1 (point size or point type) and Optional
Parameter 2 (point color / categorical grouping) into per-point
\code{pointSize}/\code{pointType}/\code{pointCol} vectors aligned with \code{ternary_points1},
plus whatever legend metadata the preview/save renderers need to draw a
matching legend (\code{param1_values}/\code{param1_bins} for the size/type legend,
\code{unique_groups}/\code{group_colors}/\code{group_counts} for a categorical color
legend). When Optional Parameter 2 is a categorical grouping, this is
also where rows outside the selected groups get dropped from
\code{ternary_points1} itself (with the point size/type vectors kept in sync) -
the reason this function takes and can return a modified
\code{ternary_points1} and \code{selected_groups}, unlike a pure "compute some
columns" helper. Extracted from \code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} as its own
function because it's the largest of that function's remaining inline
responsibilities (per the vidternary Structural Audit's §04
responsibility table) - everything here is genuinely self-contained once
\code{ternary_points1}/\code{matrika} exist, with no interaction with the
filtering or multivariate-analysis steps that ran earlier.
}
