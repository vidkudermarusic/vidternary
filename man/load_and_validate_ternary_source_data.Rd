% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_plot_data_prep.R
\name{load_and_validate_ternary_source_data}
\alias{load_and_validate_ternary_source_data}
\title{Load and validate the source Excel data for a ternary plot}
\usage{
load_and_validate_ternary_source_data(
  xlsx_file,
  element_A,
  element_B,
  element_C,
  output_format,
  preview
)
}
\arguments{
\item{xlsx_file}{Path to the uploaded \code{.xlsx} file (temp upload path).}

\item{element_A, element_B, element_C}{Ternary-axis element specs, each a
\verb{list(col = <one or more column names>)}.}

\item{output_format}{File format for a real save (e.g. \code{"png"}); only
validated here, not used to load anything.}

\item{preview}{If \code{TRUE}, suppress the \code{print(colnames(M))} console
dump this does outside preview mode.}
}
\value{
A data frame: Sheet 1 of \code{xlsx_file}, unfiltered.
}
\description{
The very first thing \code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} does: reads Sheet 1
of \code{xlsx_file}, then validates the element/output-format inputs before
any filtering or coordinate computation is attempted. Extracted as its
own function because it carries no shared mutable state with the rest
of \code{prepare_ternary_plot_data()} (per the vidternary Structural Audit's
§04 responsibility table) - its only output is the loaded data frame.
}
