% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_plot.R
\name{general_ternary_plot}
\alias{general_ternary_plot}
\title{Create (and optionally save) a ternary diagram from an Excel file}
\usage{
general_ternary_plot(
  xlsx_file,
  working_dir = getwd(),
  output_dir = NULL,
  element_A,
  element_B,
  element_C,
  optional_param1 = NULL,
  optional_param2 = NULL,
  color_palette = "blue",
  xlsx_display_name = NULL,
  preview = FALSE,
  use_mahalanobis = FALSE,
  reference_data = NULL,
  optional_param1_representation = "point_size",
  output_format = "png",
  use_isolation_forest = FALSE,
  use_iqr_filter = FALSE,
  use_zscore_filter = FALSE,
  use_mad_filter = FALSE,
  lambda = 1,
  omega = 0,
  keep_outliers_mahalanobis = FALSE,
  keep_outliers_isolation = FALSE,
  keep_outliers_iqr = FALSE,
  keep_outliers_zscore = FALSE,
  keep_outliers_mad = FALSE,
  individual_filters_A = NULL,
  individual_filters_B = NULL,
  individual_filters_C = NULL,
  custom_mdthresh = NULL,
  mdthresh_mode = "auto",
  mahalanobis_reference = "dataset2",
  selected_columns = NULL,
  include_plot_notes = TRUE,
  use_manual_point_size = FALSE,
  manual_point_size = 1,
  selected_groups = NULL,
  is_categorical_group = FALSE
)
}
\arguments{
\item{xlsx_file}{Path to the input \code{.xlsx} file (Sheet 1 is read).}

\item{working_dir}{Working directory to \code{setwd()} into for the duration
of the call (restored on exit). Default \code{getwd()}.}

\item{output_dir}{Directory to save the plot into. If \code{NULL} or
\code{preview = TRUE}, no file is saved.}

\item{element_A}{A list \verb{list(col = <column name(s)>, filter = <optional filter string>)} for vertex A.}

\item{element_B}{Same shape as \code{element_A}, for vertex B.}

\item{element_C}{Same shape as \code{element_A}, for vertex C.}

\item{optional_param1}{Optional list \code{list(col = ..., filter = ...)}
controlling point size or shape (see \code{optional_param1_representation}).}

\item{optional_param2}{Optional list \code{list(col = ..., filter = ...)} controlling point color.}

\item{color_palette}{Color palette name for \code{optional_param2}. Default \code{"blue"}.}

\item{xlsx_display_name}{Optional original filename, used for plot
titles/saved filenames instead of \code{xlsx_file}'s (often temp) basename.}

\item{preview}{If \code{TRUE}, draw only (no file saved), regardless of \code{output_dir}. Default \code{FALSE}.}

\item{use_mahalanobis}{Apply a Mahalanobis-distance outlier filter. Default \code{FALSE}.}

\item{reference_data}{Reference dataset for Mahalanobis/Isolation
Forest when \code{mahalanobis_reference} is \code{"dataset1"}/\code{"dataset2"}; must
be supplied by the caller in that case.}

\item{optional_param1_representation}{How to render \code{optional_param1}:
\code{"point_size"} or a point-shape mode. Default \code{"point_size"}.}

\item{output_format}{Saved file format: \code{"png"}, \code{"jpeg"}, \code{"pdf"}, or \code{"tiff"}. Default \code{"png"}.}

\item{use_isolation_forest}{Apply an Isolation Forest outlier filter. Default \code{FALSE}.}

\item{use_iqr_filter}{Apply an IQR outlier filter. Default \code{FALSE}.}

\item{use_zscore_filter}{Apply a Z-score outlier filter. Default \code{FALSE}.}

\item{use_mad_filter}{Apply a MAD outlier filter. Default \code{FALSE}.}

\item{lambda}{Sensitivity parameter for the automatic Mahalanobis threshold. Default 1.}

\item{omega}{Leniency parameter for the automatic Mahalanobis threshold. Default 0.}

\item{keep_outliers_mahalanobis}{If \code{TRUE}, keep only Mahalanobis
outliers instead of removing them. Default \code{FALSE}.}

\item{keep_outliers_isolation}{If \code{TRUE}, keep only Isolation Forest
outliers instead of removing them. Default \code{FALSE}.}

\item{keep_outliers_iqr}{If \code{TRUE}, keep only IQR outliers instead of removing them. Default \code{FALSE}.}

\item{keep_outliers_zscore}{If \code{TRUE}, keep only Z-score outliers instead of removing them. Default \code{FALSE}.}

\item{keep_outliers_mad}{If \code{TRUE}, keep only MAD outliers instead of removing them. Default \code{FALSE}.}

\item{individual_filters_A}{Named list of per-column filter strings for element A's column(s).}

\item{individual_filters_B}{Named list of per-column filter strings for element B's column(s).}

\item{individual_filters_C}{Named list of per-column filter strings for element C's column(s).}

\item{custom_mdthresh}{Manual Mahalanobis distance threshold, used when \code{mdthresh_mode == "manual"}.}

\item{mdthresh_mode}{Either \code{"auto"} (default) or \code{"manual"}.}

\item{mahalanobis_reference}{Reference dataset for Mahalanobis/Isolation
Forest: \code{"self"}, \code{"dataset1"}, or \code{"dataset2"}. Default \code{"dataset2"}.}

\item{selected_columns}{Character vector of numeric columns used by the
statistical/multivariate filter methods.}

\item{include_plot_notes}{Whether to include filter/parameter notes on
the plot. Default \code{TRUE}.}

\item{use_manual_point_size}{Override the automatic point size. Default \code{FALSE}.}

\item{manual_point_size}{Point size used when \code{use_manual_point_size = TRUE}. Default 1.0.}

\item{selected_groups}{Character vector restricting which categories of
a categorical \code{optional_param2} are plotted.}

\item{is_categorical_group}{Whether \code{optional_param2} should be treated
as categorical (drives group coloring/legend). Default \code{FALSE}.}
}
\value{
The path to the saved plot file (invisibly, from
\code{save_ternary_plot_to_file()}), or \code{NULL} in preview mode / when no
\code{output_dir} is set.
}
\description{
The package's main entry point: loads \code{xlsx_file}, sums the columns for
each of elements A/B/C, applies any per-element filters plus at most one
statistical/multivariate outlier method (if more than one \verb{use_*} flag
is \code{TRUE}, only the highest-priority one is kept - see this function's
inline comment for the priority order), computes ternary coordinates,
draws the plot to the active graphics device, and - unless \code{preview = TRUE} or \code{output_dir} is \code{NULL} - saves it to a file.
}
