% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compositional_data_analysis.R
\name{ilr_transform}
\alias{ilr_transform}
\title{Isometric log-ratio (ILR) transform of compositional columns}
\usage{
ilr_transform(data, parts, zero_replacement = NULL)
}
\arguments{
\item{data}{A data frame containing the compositional columns.}

\item{parts}{Character vector of column names to transform (at least 2).
The order determines what each \code{ilr_j} balance represents.}

\item{zero_replacement}{Value substituted for zero/NA entries before
taking logs. Defaults to half the smallest positive value found across
\code{parts}.}
}
\value{
A list with \code{ilr} (data frame, columns \code{ilr_1..ilr_(D-1)}),
\code{basis} (the \verb{D x (D-1)} orthonormal basis matrix \code{V} such that
\code{clr = V \%*\% t(ilr)}), and \code{parts}.
}
\description{
An orthonormal re-expression of the CLR coordinates into \code{D-1}
coordinates with a non-singular covariance matrix, using the standard
sequential-binary-partition ("pivot coordinate") basis: coordinate \code{j}
contrasts the geometric mean of the first \code{j} parts against part \code{j+1}.
ILR is an isometry of CLR, so PCA on either basis gives identical
variance-explained and scores (up to sign) - only the loadings'
interpretation differs (per-element for CLR, an abstract balance for ILR).
}
