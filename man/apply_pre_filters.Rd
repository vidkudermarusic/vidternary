% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_pre_analysis_filter.R
\name{apply_pre_filters}
\alias{apply_pre_filters}
\title{Apply a set of pre-analysis column filters to a data frame (AND logic)}
\usage{
apply_pre_filters(df, filters)
}
\arguments{
\item{df}{A data frame.}

\item{filters}{A named list of filter condition strings, e.g. from
\code{collect_pre_filters()} - \code{list(Area = "> 1", "Fe (wt.\%)" = "> 10")}.}
}
\value{
The filtered data frame (\code{df} unchanged if \code{filters} is empty).
}
\description{
Applies each column's filter condition in sequence via \code{apply_filter()}
\itemize{
\item a row must satisfy every one to survive. Column names and filter
syntax errors are both reported with the specific column named, rather
than \code{apply_filter()}'s own column-agnostic message alone.
}
}
