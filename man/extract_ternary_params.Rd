% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_filters.R
\name{extract_ternary_params}
\alias{extract_ternary_params}
\title{Build the full parameter list for \code{general_ternary_plot()} from Shiny inputs}
\usage{
extract_ternary_params(
  input,
  rv,
  dataset_num,
  preview = FALSE,
  multiple_mode = FALSE
)
}
\arguments{
\item{input}{The Shiny \code{input} object.}

\item{rv}{The app's shared \code{reactiveValues} object.}

\item{dataset_num}{Dataset number (1 or 2); ignored when \code{multiple_mode = TRUE}.}

\item{preview}{Whether this is for a live preview (\code{TRUE}) or an actual
save (\code{FALSE}). Default \code{FALSE}.}

\item{multiple_mode}{Whether to read the Multiple Ternary Creator's
inputs instead of the main Ternary Plots tab's. Default \code{FALSE}.}
}
\value{
A named list of parameters ready to pass to \code{general_ternary_plot()},
or \code{NULL} if element A/B/C aren't all set yet. \code{output_dir} defaults to
\code{NULL} (no save) - every real save path (a \code{downloadHandler}'s own
\code{content()} function) overrides it explicitly with a fresh temp
directory before calling \code{general_ternary_plot()}, the same way the
Multiple Ternary Creator's batch handler already does.
}
\description{
Reads elements A/B/C, optional parameters 1/2, all element/optional-param
filters, the active statistical/multivariate filter method and its
settings, output options, and group-selection state from \code{input}/\code{rv},
assembling them into the single list \code{general_ternary_plot()} expects.
Used by both the main Ternary Plots tab (\code{multiple_mode = FALSE}) and
the Multiple Ternary Creator (\code{multiple_mode = TRUE}).
}
