% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_plot_data_prep.R
\name{compute_ternary_coordinates}
\alias{compute_ternary_coordinates}
\title{Compute and validate a ternary plot's normalized A/B/C coordinates}
\usage{
compute_ternary_coordinates(
  M,
  all_selected_elements,
  element_A,
  element_B,
  element_C,
  optional_param1,
  optional_param2,
  use_mahalanobis,
  reference_data
)
}
\arguments{
\item{M}{The data frame to compute coordinates from (already
loaded/filtered by the earlier pipeline stages).}

\item{all_selected_elements}{Character vector of every column selected
across \code{element_A}/\code{B}/\code{C} (as built earlier in
\code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}}, before this extraction's own call).}

\item{element_A, element_B, element_C}{Ternary-axis element specs, each a
\verb{list(col = <one or more column names>)}.}

\item{optional_param1, optional_param2}{Optional \verb{list(col = <column name(s)>, ...)} specs, included in \code{needed_columns}/\code{optional_columns}
when supplied.}

\item{use_mahalanobis, reference_data}{Used only by this function's own
\code{getOption("ternary.debug", FALSE)} diagnostic \code{cat()} calls (printing
a data sample when Mahalanobis filtering against a reference dataset
was active) - not read for any control-flow decision here.}
}
\value{
This function's entire local environment as a list
(\code{as.list(environment())}) - \code{matrika} and \code{ternary_points1} are the
fields \code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} and the preview/save renderers
actually read back; the rest (\code{needed_columns}, \code{element_columns},
\code{optional_columns}, \code{A_values}/\code{B_values}/\code{C_values}, \code{total_values},
\code{valid_rows}, and the possibly-further-filtered \code{M}) are echoed back
unchanged from how they already existed in
\code{prepare_ternary_plot_data()}'s own environment before this
extraction.
}
\description{
Builds \code{matrika} (the numeric-plus-optional-parameter working data frame:
selects \code{needed_columns}, drops zero-sum rows, \code{na.omit()}s, then splits
\code{element_columns} vs. \code{optional_columns}), sums each element's selected
column(s) into \code{A_values}/\code{B_values}/\code{C_values}, normalizes by their row
total into \code{ternary_points1}, and drops any row whose total is zero or
whose resulting coordinate is \code{NA}/infinite from both \code{M} and
\code{ternary_points1} - erroring if a needed column is missing from the data,
or if nothing survives validation. Extracted from
\code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} as its own function because it's the last
and most central of that function's identified responsibilities (per
the vidternary Structural Audit's §04 responsibility table) - tackled
last of the seven extractions on this function, once every other piece
it interacts with (filtering, multivariate dispatch, point styling,
title assembly) had already been extracted and verified.
}
