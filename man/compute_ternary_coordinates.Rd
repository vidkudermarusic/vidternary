% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_plot_coordinates.R
\name{compute_ternary_coordinates}
\alias{compute_ternary_coordinates}
\title{Compute and validate a ternary plot's normalized A/B/C coordinates}
\usage{
compute_ternary_coordinates(
  M,
  all_selected_elements,
  element_A,
  element_B,
  element_C,
  optional_param1,
  optional_param2,
  use_mahalanobis,
  reference_data
)
}
\arguments{
\item{M}{The data frame to compute coordinates from (already
loaded/filtered by the earlier pipeline stages).}

\item{all_selected_elements}{Character vector of every column selected
across \code{element_A}/\code{B}/\code{C} (as built earlier in
\code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}}, before this extraction's own call).}

\item{element_A, element_B, element_C}{Ternary-axis element specs, each a
\verb{list(col = <one or more column names>)}. A \emph{partial} overlap between
two elements' column sets is fine and common in real compositional
data (e.g. A = Fe+O, B = Al+O, C = Ti - O legitimately contributes to
more than one vertex) - but no two of the three may select the exact
same \emph{complete} set (e.g. A = O, B = O), which collapses the ternary
diagram onto a single edge/point and is rejected with a \code{stop()}.}

\item{optional_param1, optional_param2}{Optional \verb{list(col = <column name(s)>, ...)} specs, included in \code{needed_columns}/\code{optional_columns}
when supplied.}

\item{use_mahalanobis, reference_data}{Used only by this function's own
\code{getOption("ternary.debug", FALSE)} diagnostic \code{cat()} calls (printing
a data sample when Mahalanobis filtering against a reference dataset
was active) - not read for any control-flow decision here.}
}
\value{
This function's entire local environment as a list
(\code{as.list(environment())}) - \code{matrika} and \code{ternary_points1} are the
fields \code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} and the preview/save renderers
actually read back, and now always share the same row set/order as
\code{M}. The rest (\code{needed_columns}, \code{element_columns},
\code{optional_columns}, \code{A_values}/\code{B_values}/\code{C_values}, \code{total_values},
\code{valid_rows}) are echoed back for diagnostic/debug use.
}
\description{
Builds \code{matrika} (the numeric-plus-optional-parameter working data frame:
selects \code{needed_columns}, splits \code{element_columns} vs. \code{optional_columns},
converts the former to numeric), sums each element's selected column(s)
into \code{A_values}/\code{B_values}/\code{C_values}, and normalizes by their row total
into \code{ternary_points1} - erroring if a needed column is missing from the
data, or if nothing survives validation.
}
\section{One shared \code{valid_rows} mask}{

A row is valid when its element total is finite and positive, AND no
needed column (any element sub-column, or either optional parameter's
column) is \code{NA} for that row - a genuinely missing measurement is not
silently treated as zero and plotted as if the composition were fully
known. That single mask is computed once and applied identically to
\code{matrika}, \code{M}, and \code{ternary_points1}, so all three end up with the
exact same rows in the exact same order. This matters because
\code{\link[=compute_point_styling]{compute_point_styling()}} later reads Optional Param 1/2 values out of
\code{matrika} and applies them \emph{positionally} against \code{ternary_points1} -
row validity must be decided in exactly one place so the two objects
never drift out of alignment.
}

