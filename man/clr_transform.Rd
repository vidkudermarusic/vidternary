% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compositional_data_analysis.R
\name{clr_transform}
\alias{clr_transform}
\title{Centered log-ratio (CLR) transform of compositional columns}
\usage{
clr_transform(data, parts, zero_replacement = NULL)
}
\arguments{
\item{data}{A data frame containing the compositional columns.}

\item{parts}{Character vector of column names to transform (at least 2).}

\item{zero_replacement}{Value substituted for zero/NA entries before
taking logs. Defaults to half the smallest positive value found across
\code{parts}.}
}
\value{
A data frame of CLR-transformed values, one column per \code{parts} entry.
}
\description{
\code{clr_i = ln(x_i) - mean(ln(x))}. Each output column still corresponds to
one original element, so loadings/biplots computed from it stay directly
interpretable - but the transformed columns always sum to 0 per row, so
their covariance matrix is singular (see \code{ilr_transform()}).
}
