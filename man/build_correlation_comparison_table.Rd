% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats_display_utils.R
\name{build_correlation_comparison_table}
\alias{build_correlation_comparison_table}
\title{Compare the internal correlation structure of 2+ datasets}
\usage{
build_correlation_comparison_table(dfs, common_cols)
}
\arguments{
\item{dfs}{A named list of 2+ data frames; names become the \code{Dataset} column's values.}

\item{common_cols}{Numeric columns present in all of \code{dfs}, to correlate. Needs at least 2.}
}
\value{
A data frame: \code{Dataset}, \code{Variable_1}, \code{Variable_2}, \code{Correlation}.
}
\description{
\code{build_correlation_pairs_table()} for each dataset over \code{common_cols},
stacked long (one \code{Dataset} column) so it stays a single sortable table
regardless of dataset count. Does not compute a direct row-wise
correlation \emph{between} datasets - that isn't meaningful when datasets
have different row counts (independent samples with no natural pairing).
}
