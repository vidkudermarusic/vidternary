% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extreme_value_analysis.R
\name{assign_control_areas}
\alias{assign_control_areas}
\title{Assign fields to control areas of \code{fields_per_area} fields each}
\usage{
assign_control_areas(field, fields_per_area = 1, all_fields = field)
}
\arguments{
\item{field}{Field ID of each row to assign.}

\item{fields_per_area}{Whole number of fields per control area (>= 1).}

\item{all_fields}{Field IDs of all inspected rows, used to count the
fields (defaults to \code{field}; pass the unfiltered data's IDs so rows
removed by a filter don't shrink the inspected area).}
}
\value{
A list: \code{control_area} (per-row control-area number, \code{NA} for
rows left out or with an unmatched ID), \code{n_fields}, \code{n_control_areas},
\code{n_leftover_fields}, \code{numeric_ids}.
}
\description{
Whole-number field IDs are taken as consecutively numbered fields, so
\verb{min(id)..max(id)} counts as the inspected fields (a missing number is a
field with no detected particle) and fields are grouped by number
(\verb{1..k}, \verb{k+1..2k}, ...). Any other IDs are grouped in sorted order of
the distinct values. Fields in an incomplete last group are left out.
}
