% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_plot_data_prep.R
\name{apply_element_and_parameter_filters}
\alias{apply_element_and_parameter_filters}
\title{Apply per-element and optional-parameter filters to a ternary plot's data}
\usage{
apply_element_and_parameter_filters(
  M,
  element_A,
  element_B,
  element_C,
  individual_filters_A,
  individual_filters_B,
  individual_filters_C,
  optional_param1,
  optional_param2,
  preview
)
}
\arguments{
\item{M}{The data frame to filter (already loaded/validated).}

\item{element_A, element_B, element_C}{Ternary-axis element specs, each a
\verb{list(col = <one or more column names>, filter = <filter string or NULL/list>)}.}

\item{individual_filters_A, individual_filters_B, individual_filters_C}{Named lists (by column) of per-element filter strings, as built by
\code{collect_main_ternary_filters()}; \code{NULL}/empty falls back to each
element's own single \code{filter} value applied to every selected column.}

\item{optional_param1, optional_param2}{Optional \verb{list(col = <column name(s)>, filter = <filter string or NULL>)} for point size/type and
point color / categorical grouping, respectively.}

\item{preview}{If \code{TRUE}, suppress the console progress messages
\code{apply_individual_filters()} and this function's own filter loops print.}
}
\value{
This function's entire local environment as a list
(\code{as.list(environment())}) - \code{M} (filtered) and \code{all_selected_elements}
are the fields \code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} actually reads back; the
rest are this block's own internal working variables (including its
three local closures), echoed back unchanged from how they already
existed in \code{prepare_ternary_plot_data()}'s own environment before this
extraction.
}
\description{
Applies element A/B/C's per-column filters (via \code{apply_individual_filters()}
below - either one shared filter string applied to every selected column,
or genuinely independent per-column filters, depending on whether
\code{individual_filters_A}/\code{B}/\code{C} is supplied), then \code{optional_param1}'s and
\code{optional_param2}'s own single filter string, if either has one. Defines
\code{parse_filter_condition()}, \code{apply_filter()}, and \code{apply_individual_filters()}
as its own \emph{local} closures, exactly as in the original single-function
version of this code - they are NOT the same functions as the
same-named ones in \code{helpers.R}/\code{helpers_filters.R} (different,
ternary-plot-specific behavior), so they must stay local rather than
becoming top-level functions, to avoid silently shadowing those
unrelated global functions package-wide (a real instance of exactly
this bug class, unrelated to these, was found and fixed three times
elsewhere in this package - see the vidternary Structural Audit's
§03/§08). \code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} keeps its own two sibling local
closures (\code{preview_title_layout()}, \code{calculate_plot_dimensions()})
untouched in its own body - neither is used by filtering, and both are
still needed there (as \code{build_ternary_plot_title()}'s \code{title_layout_fn}
callback, and echoed into \code{pd} for \code{ternary_plot_save.R}'s own use).
Extracted from \code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} as its own function per the
vidternary Structural Audit's §04 responsibility table.
}
