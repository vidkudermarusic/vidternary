% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comprehensive_analysis.R
\name{run_comprehensive_analysis}
\alias{run_comprehensive_analysis}
\title{Run the full programmatic analysis pipeline on two datasets}
\usage{
run_comprehensive_analysis(
  data1,
  data2,
  analysis_config = NULL,
  output_dir = NULL
)
}
\arguments{
\item{data1}{First dataset, as a data frame.}

\item{data2}{Second dataset, as a data frame.}

\item{analysis_config}{Optional list of analysis configuration, stored
on the result but not currently read by this function.}

\item{output_dir}{Directory to write generated files into. Defaults to
\verb{analysis_output/} under the current working directory; created if
missing.}
}
\value{
A list of results: \code{quality_report}, \code{quality_dashboard},
\code{visualizations}, \code{statistics}, \code{mahalanobis_standard},
\code{isolation_forest}, \code{export_files}, and \code{final_report} (path to the
generated HTML report).
}
\description{
Orchestrates data-quality assessment, correlation/distribution
visualizations, descriptive statistics, multivariate analysis
(Mahalanobis, Isolation Forest), CSV export, and an HTML report - all in
one call. This is a programmatic entry point only: it is not wired into
the Shiny app's UI (no tab or button calls it), so it must be invoked
directly from R, e.g. \code{run_comprehensive_analysis(df1, df2)}.
}
