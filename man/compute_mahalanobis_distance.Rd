% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate.R
\name{compute_mahalanobis_distance}
\alias{compute_mahalanobis_distance}
\title{Compute Mahalanobis distances and flag outliers}
\usage{
compute_mahalanobis_distance(
  data1,
  data2,
  lambda = 1,
  omega = 0,
  keep_outliers = FALSE,
  custom_mdthresh = NULL,
  selected_columns,
  mdthresh_mode = "auto"
)
}
\arguments{
\item{data1}{Dataset whose points are scored, as a data frame.}

\item{data2}{Reference dataset the distribution is fit to, as a data frame.}

\item{lambda}{Sensitivity parameter for the automatic threshold. Default 1.}

\item{omega}{Leniency parameter for the automatic threshold. Default 0.}

\item{keep_outliers}{Currently stored on the result but not used to
filter within this function. Default \code{FALSE}.}

\item{custom_mdthresh}{Manual distance threshold, used when \code{mdthresh_mode == "manual"}.}

\item{selected_columns}{Character vector of at least 2 numeric column names to use.}

\item{mdthresh_mode}{Either \code{"auto"} (default) or \code{"manual"}.}
}
\value{
A list: \code{distances}, \code{MDthresh}, \code{MDmean}, \code{stdMD}, \code{lambda},
\code{omega}, \code{outlier_95}/\code{outlier_99}/\code{outlier_custom} (counts),
\code{total_points}, \code{df}, \code{common_cols}, \code{outlier_indices},
\code{keep_outliers}, \code{threshold_method}, \code{threshold_formula}.
}
\description{
Computes the Mahalanobis distance of each \code{data1} row from \code{data2}'s
distribution over \code{selected_columns}, then flags outliers against
either a manual threshold or the automatic formula \code{MDthresh = MDmean + sqrt(100/(100 + lambda - omega)) * stdMD} (Vode et al., 2022,
\url{https://doi.org/10.3390/ma15020684}).
}
