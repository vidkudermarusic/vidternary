% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_plot_data_prep.R
\name{resolve_ternary_output_directory}
\alias{resolve_ternary_output_directory}
\title{Resolve a ternary plot's output folder and file-base name}
\usage{
resolve_ternary_output_directory(
  xlsx_file,
  xlsx_display_name,
  output_dir,
  preview,
  working_dir
)
}
\arguments{
\item{xlsx_file}{Path to the uploaded \code{.xlsx} file (temp upload path).}

\item{xlsx_display_name}{Optional original filename, preferred over
\code{xlsx_file}'s temp-upload basename.}

\item{output_dir}{Base output directory for a real (non-preview) save,
or \code{NULL} to use the original per-charge-subfolder logic.}

\item{preview}{If \code{TRUE}, this call is only feeding a live preview
render, not a save - no directory is created.}

\item{working_dir}{Directory to resolve relative paths against, passed
through to \code{\link[=create_ternary_output_dir]{create_ternary_output_dir()}}.}
}
\value{
This function's entire local environment as a list
(\code{as.list(environment())}) - \code{custom_folder}, \code{file_base}, and
\code{plot_folder_name} are the fields that existed in
\code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}}'s own environment before this
extraction; \code{dir_info} is this function's own internal working
variable in the \code{create_ternary_output_dir()} branch, echoed back
unchanged.
}
\description{
Two distinct code paths, both producing \code{custom_folder}, \code{file_base},
and \code{plot_folder_name}: when \code{output_dir} is supplied for a real
(non-preview) save, uses \code{output_dir} directly (the Multiple Ternary
Creator's "one shared folder for every plot" behavior), routing the
display name through \code{\link[=extract_file_base]{extract_file_base()}} (\code{file_management.R}) so a
crafted upload filename containing path separators can't steer the save
outside \code{output_dir}; otherwise delegates to
\code{\link[=create_ternary_output_dir]{create_ternary_output_dir()}} (\code{file_management.R}), which creates a
per-charge subfolder (with a timestamp suffix if one already exists) or
returns \code{custom_folder = NULL} in preview mode. Extracted from
\code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} as its own function because it's fully
self-contained (per the vidternary Structural Audit's §04 responsibility
table) - of its three outputs, only \code{file_base} is read again by this
function's caller (passed to \code{\link[=build_ternary_plot_title]{build_ternary_plot_title()}} for the
title's "charge" line); \code{custom_folder}/\code{plot_folder_name} are computed
but not read by any downstream consumer today, exactly as before this
extraction - a pure reorganization, not a cleanup of that.
}
