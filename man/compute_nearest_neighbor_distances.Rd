% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_clustering_analysis.R
\name{compute_nearest_neighbor_distances}
\alias{compute_nearest_neighbor_distances}
\title{Two methods, both exact (verified to agree to floating-point precision -
\code{"kdtree"} is not an approximation): \code{"matrix"} builds the full n x n
pairwise distance matrix (\code{O(n^2)} time \emph{and} memory - at n=16705 that's
a ~2.2GB matrix, rebuilt on every Monte Carlo simulation in
\code{clark_evans_test()}, which is minutes-to-tens-of-minutes slow).
\code{"kdtree"} uses \code{RANN::nn2()} (\verb{O(n log n)}), ~150-250x faster at a few
thousand points and the only practical option much above that - kept
as an option alongside \code{"matrix"} rather than replacing it outright.}
\usage{
compute_nearest_neighbor_distances(x, y, method = c("kdtree", "matrix"))
}
\arguments{
\item{x}{Numeric vector of X coordinates.}

\item{y}{Numeric vector of Y coordinates (same length as \code{x}). Requires
at least 2 points.}

\item{method}{\code{"kdtree"} (default; fast, \code{RANN::nn2()}) or \code{"matrix"}
(slow for large \code{n}, full distance matrix via \code{stats::dist()}).}
}
\value{
A numeric vector (same length as \code{x}), each point's distance to
its nearest other point.
}
\description{
Two methods, both exact (verified to agree to floating-point precision -
\code{"kdtree"} is not an approximation): \code{"matrix"} builds the full n x n
pairwise distance matrix (\code{O(n^2)} time \emph{and} memory - at n=16705 that's
a ~2.2GB matrix, rebuilt on every Monte Carlo simulation in
\code{clark_evans_test()}, which is minutes-to-tens-of-minutes slow).
\code{"kdtree"} uses \code{RANN::nn2()} (\verb{O(n log n)}), ~150-250x faster at a few
thousand points and the only practical option much above that - kept
as an option alongside \code{"matrix"} rather than replacing it outright.
}
