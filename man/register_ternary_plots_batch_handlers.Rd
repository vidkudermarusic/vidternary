% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server_ternary_plots_batch.R
\name{register_ternary_plots_batch_handlers}
\alias{register_ternary_plots_batch_handlers}
\title{Wire up the "Multiple Ternary Creator" tab's batch server logic}
\usage{
register_ternary_plots_batch_handlers(
  input,
  output,
  session,
  rv,
  show_message,
  log_operation
)
}
\arguments{
\item{input}{The Shiny \code{input} object.}

\item{output}{The Shiny \code{output} object.}

\item{session}{The Shiny session object.}

\item{rv}{The app's shared \code{reactiveValues} object.}

\item{show_message}{Function to show a user-facing status message.}

\item{log_operation}{Function to record a structured log entry.}
}
\value{
Not meaningful (whatever its last statement happens to
evaluate to, with no explicit \code{return()}) - called for its side
effect of registering observers/outputs.
}
\description{
Registers the multi-file batch handlers: uploading several \code{.xlsx}
files, the per-element dynamic filter UI (\code{multiple_filters_A}/\code{B}/\code{C}),
and "Create & Save all ternary plots (zip)" - generating one ternary
plot per uploaded file from one shared set of element/filter parameters
(via \code{extract_ternary_params(..., multiple_mode = TRUE)}), zipped into
one browser download.
Called directly from \code{server_logic.R}'s own
\code{moduleServer("multiple_ternary", ...)}, independent of the "Ternary
Plots" tab's own module/namespace.
}
