% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_utils_builder.R
\name{build_custom_plot}
\alias{build_custom_plot}
\title{Build a Plot Builder chart from arbitrary column names}
\usage{
build_custom_plot(
  data,
  type,
  x,
  y = NULL,
  color_by = "none",
  log_x = FALSE,
  log_y = FALSE,
  percent = FALSE,
  bar_values = NULL,
  rose_bin_width = 10,
  hist_bins = 30
)
}
\arguments{
\item{data}{A data frame.}

\item{type}{Chart type: \code{"violin"}, \code{"box"}, \code{"bar"}, \code{"hist"},
\code{"scatter"}, or \code{"rose"}.}

\item{x}{Name of the X-axis / grouping / direction column, depending on \code{type}.}

\item{y}{Name(s) of the Y-axis / value column(s). A character vector of
length > 1 is only meaningful for \code{"violin"}/\code{"box"}.}

\item{color_by}{Name of a column to color/group by, or \code{"none"} to disable. Default \code{"none"}.}

\item{log_x}{Log-scale the X axis (ignored for \code{"rose"}). Default \code{FALSE}.}

\item{log_y}{Log-scale the Y axis (ignored for \code{"rose"}). Default \code{FALSE}.}

\item{percent}{For \code{type = "bar"}, show percentages instead of raw counts. Default \code{FALSE}.}

\item{bar_values}{Optional character vector restricting which
categories are shown for \code{type = "bar"}.}

\item{rose_bin_width}{Bin width in degrees, for \code{type = "rose"}. Default 10.}

\item{hist_bins}{Number of bins, for \code{type = "hist"}. Default 30.}
}
\value{
A \code{ggplot} object.
}
\description{
The generic, schema-agnostic chart builder behind the "Plot Builder"
tab: violin, box, bar (counts), histogram, scatter, or rose diagram,
with every axis/color argument a plain column-name string resolved via
\code{rlang::sym()}. Selecting multiple \code{y} columns for \code{type} \code{"violin"}/
\code{"box"} reshapes the data to compare them side by side within each \code{x} group.
}
