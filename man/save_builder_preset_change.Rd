% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_builder_presets.R
\name{save_builder_preset_change}
\alias{save_builder_preset_change}
\title{Apply one preset change against whatever is currently saved on disk}
\usage{
save_builder_preset_change(mutate)
}
\arguments{
\item{mutate}{A function taking the freshly-loaded on-disk presets list
and returning the updated list to save. Called with the real current
contents, not the caller's own possibly-stale in-memory copy.}
}
\value{
The updated, already-saved presets list - store this back into
the caller's own \code{rv$plot_presets} so its in-memory copy reflects
what's now really on disk, not just its own one change.
}
\description{
\code{PLOT_BUILDER_PRESETS_FILE} is one bare path shared by every Shiny
session in the same R process; each session's own \code{rv$plot_presets} is
loaded once at server-creation time and never refreshed. Re-reading the
file immediately before writing keeps a save from clobbering a preset a
\emph{different} session wrote in the meantime - narrow enough that a single
shared JSON file for a local-first Shiny app doesn't warrant real file
locking on top of it.
}
