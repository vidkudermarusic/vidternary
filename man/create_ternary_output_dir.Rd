% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/file_management.R
\name{create_ternary_output_dir}
\alias{create_ternary_output_dir}
\title{Resolve/create the output folder for a ternary plot save}
\usage{
create_ternary_output_dir(
  xlsx_file,
  xlsx_display_name = NULL,
  output_dir = NULL,
  preview = FALSE,
  working_dir = getwd()
)
}
\arguments{
\item{xlsx_file}{Path to the uploaded \code{.xlsx} file.}

\item{xlsx_display_name}{Optional original filename, passed to \code{extract_file_base()}.}

\item{output_dir}{Base output directory. If \code{NULL} and not in preview
mode, falls back to \verb{<working_dir>/plots2}.}

\item{preview}{If \code{TRUE}, skip directory creation entirely.}

\item{working_dir}{Directory to temporarily \code{setwd()} into for relative
path resolution; restored on exit. Default \code{getwd()}.}
}
\value{
A list: \code{custom_folder} (path, or \code{NULL} in preview mode),
\code{file_base}, \code{plot_folder_name}.
}
\description{
In preview mode, no directory is created. Otherwise creates
\verb{<output_dir>/charge<file_base>} (falling back to \verb{<cwd>/plots2/...} if
\code{output_dir} is \code{NULL}), appending a timestamp if that folder already exists.
}
