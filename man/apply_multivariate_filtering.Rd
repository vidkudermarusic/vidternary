% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_plot_data_prep.R
\name{apply_multivariate_filtering}
\alias{apply_multivariate_filtering}
\title{Apply Mahalanobis-distance or Isolation-Forest outlier filtering}
\usage{
apply_multivariate_filtering(
  M,
  use_mahalanobis,
  use_isolation_forest,
  selected_columns,
  mahalanobis_reference,
  reference_data,
  preview,
  keep_outliers_isolation,
  keep_outliers_mahalanobis,
  lambda,
  omega,
  custom_mdthresh,
  mdthresh_mode
)
}
\arguments{
\item{M}{The data frame to filter (already loaded/individually filtered).}

\item{use_mahalanobis, use_isolation_forest}{Which method, if either, is active.}

\item{selected_columns}{Character vector of at least 2 numeric column
names to use; mandatory whenever either method is active.}

\item{mahalanobis_reference}{Which dataset the reference distribution is
fit to: \code{"self"}, \code{"dataset1"}, or \code{"dataset2"}.}

\item{reference_data}{Reference dataset for the \code{"dataset1"}/\code{"dataset2"}
modes; must be supplied by the caller in that case.}

\item{preview}{If \code{TRUE}, suppress the console progress/status messages.}

\item{keep_outliers_isolation, keep_outliers_mahalanobis}{If \code{TRUE} for
the active method, keep only the flagged outliers instead of removing them.}

\item{lambda, omega}{Sensitivity/leniency parameters for the automatic
Mahalanobis threshold formula (see \code{\link[=compute_mahalanobis_distance]{compute_mahalanobis_distance()}}).}

\item{custom_mdthresh}{Manual Mahalanobis distance threshold, used when
\code{mdthresh_mode == "manual"}.}

\item{mdthresh_mode}{\code{"auto"} or \code{"manual"} Mahalanobis threshold mode.}
}
\value{
This function's entire local environment as a list
(\code{as.list(environment())}) - \code{M}, \code{mahal_result}, and \code{iso_result} are
the fields \code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} actually reads back; the rest
are this block's own internal working variables, echoed back unchanged
from how they already existed in \code{prepare_ternary_plot_data()}'s own
environment before this extraction.
}
\description{
Resolves the reference dataset (self / Dataset 1 / Dataset 2), runs
whichever of \code{\link[=compute_mahalanobis_distance]{compute_mahalanobis_distance()}}/\code{\link[=compute_isolation_forest]{compute_isolation_forest()}}
is active, and re-filters \code{M} to the kept rows. Skips silently (leaving
\code{M} untouched) if neither \code{use_mahalanobis} nor \code{use_isolation_forest} is
\code{TRUE}, or if the requested reference dataset isn't available; a genuine
computation error (e.g. too few usable rows/columns) is also caught and
left as an unfiltered \code{M}, with a console message - only the two
mandatory-column-selection checks below raise a real, propagating error.
Extracted from \code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} as its own function because
it's the most self-contained of that function's remaining
responsibilities (per the vidternary Structural Audit's §04
responsibility table) - its only real outputs are \code{M}, \code{mahal_result},
and \code{iso_result}.
}
