% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_filters.R
\name{collect_filters}
\alias{collect_filters}
\title{Collect per-element filter values from Shiny \code{input}}
\usage{
collect_filters(
  elements,
  filter_type,
  input,
  prefix = "filter",
  dataset_num = NULL
)
}
\arguments{
\item{elements}{Character vector of element/column names to look up filters for.}

\item{filter_type}{Filter-type label used in the input ID (e.g. \code{"A"}, \code{"op1"}).}

\item{input}{The Shiny \code{input} object.}

\item{prefix}{Input ID prefix, e.g. \code{"filter"} or \code{"multiple_filter"}. Default \code{"filter"}.}

\item{dataset_num}{Dataset number suffix (1 or 2), used only when \code{prefix == "filter"}.}
}
\value{
A named list of non-empty filter values, keyed by element name.
}
\description{
For each name in \code{elements}, looks up the matching dynamically-named
filter text input (built from \code{prefix}/\code{filter_type}/\code{dataset_num}/the
element name) and includes it in the result if non-empty.
}
