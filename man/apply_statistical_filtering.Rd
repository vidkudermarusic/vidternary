% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_plot_data_prep.R
\name{apply_statistical_filtering}
\alias{apply_statistical_filtering}
\title{Apply IQR/Z-score/MAD statistical outlier filtering}
\usage{
apply_statistical_filtering(
  M,
  use_iqr_filter,
  use_zscore_filter,
  use_mad_filter,
  selected_columns,
  keep_outliers_iqr,
  keep_outliers_zscore,
  keep_outliers_mad,
  stat_filter_log10 = FALSE
)
}
\arguments{
\item{M}{The data frame to filter (already loaded/individually filtered).}

\item{use_iqr_filter, use_zscore_filter, use_mad_filter}{Which method(s),
if any, are active.}

\item{selected_columns}{Character vector of at least 2 numeric column
names to use; mandatory whenever any method is active.}

\item{keep_outliers_iqr, keep_outliers_zscore, keep_outliers_mad}{If
\code{TRUE} for the active method, keep only the flagged outliers instead
of removing them.}

\item{stat_filter_log10}{If \code{TRUE}, every active fence is fitted and
applied on \code{log10(value)} rather than the raw value - the appropriate
choice for the right-skewed inclusion measurements this app filters.
Default \code{FALSE} (raw-scale fences, the original behaviour).}
}
\value{
This function's entire local environment as a list
(\code{as.list(environment())}) - \code{M} is the only field
\code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} actually reads back.
}
\description{
Dispatches to whichever of \code{\link[=apply_iqr_filter]{apply_iqr_filter()}}/\code{\link[=apply_zscore_filter]{apply_zscore_filter()}}/
\code{\link[=apply_mad_filter]{apply_mad_filter()}} (\code{statistical_filters.R}) is active, applying each
in turn with this app's fixed multiplier/threshold constants (IQR:
\code{1.5}, Z-score/MAD: \code{3}). Only one method is meant to be active per plot
\itemize{
\item enforced upstream in \code{general_ternary_plot()}, not here - but this
function has no mutual-exclusion guard of its own and will run all
three in sequence if somehow more than one flag is set, matching the
original inline code's own behavior exactly (a pure lift, not a
rewrite). Extracted from \code{\link[=prepare_ternary_plot_data]{prepare_ternary_plot_data()}} as its own
function because it's the most self-contained of that function's
remaining responsibilities (per the vidternary Structural Audit's Sec.04
responsibility table) - it only reads \code{M} and the filter flags/
parameters below, and its only real output is \code{M}.
}
}
\details{
Every active method is run over \emph{all} of \code{selected_columns} and a row is
dropped (or kept, if \verb{keep_outliers_*}) when it crosses the fence in
\emph{any one} of them - so the effective per-row false-positive rate rises
with the number of selected columns. See the scientific caveats in
\code{statistical_filters.R}'s source header for that and for the
skew/small-sample limitations of the individual fences.
}
