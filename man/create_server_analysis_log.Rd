% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server_analysis_log.R
\name{create_server_analysis_log}
\alias{create_server_analysis_log}
\title{Wire up the "Analysis Log" tab's server logic}
\usage{
create_server_analysis_log(
  input,
  output,
  session,
  rv,
  show_message,
  log_operation
)
}
\arguments{
\item{input}{The Shiny \code{input} object.}

\item{output}{The Shiny \code{output} object.}

\item{session}{The Shiny session object.}

\item{rv}{The app's shared \code{reactiveValues} object (reads
\code{rv$analysis_log}, the entries every tab's \code{\link[=log_operation]{log_operation()}} call
appends to).}

\item{show_message}{Function to show a user-facing status message.}

\item{log_operation}{Function to record a structured log entry.}
}
\value{
An empty list - this function's effect is entirely the
observers/outputs it registers.
}
\description{
Registers the "Clear Log" handler, the level/search filtering
(\code{input$log_level}/\code{input$search_log}, applied via a shared
\code{filtered_log_entries()} helper so the on-screen display and "Export
Log" can never disagree about which entries are shown), the on-screen
log display (\code{output$analysis_log}), the filtered entry-count summary
(\code{output$log_stats}), and the "Export Log" download handler. There used
to also be a separate "Save Log to File" button that wrote the same
entries to a pre-chosen server-side folder - removed as a fully
redundant duplicate of "Export Log" (see the vidternary Structural
Audit's §03), not converted to anything, once the global directory
picker it depended on went away.
}
