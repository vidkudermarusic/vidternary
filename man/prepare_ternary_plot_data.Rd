% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_plot_data_prep.R
\name{prepare_ternary_plot_data}
\alias{prepare_ternary_plot_data}
\title{Load, filter, and prepare data for a single ternary plot}
\usage{
prepare_ternary_plot_data(
  xlsx_file,
  working_dir,
  output_dir,
  element_A,
  element_B,
  element_C,
  optional_param1,
  optional_param2,
  color_palette,
  xlsx_display_name,
  preview,
  use_mahalanobis,
  reference_data,
  optional_param1_representation,
  output_format,
  use_isolation_forest,
  isolation_ntrees = 200,
  isolation_contamination = 0.1,
  isolation_sample_size = NULL,
  use_iqr_filter,
  use_zscore_filter,
  use_mad_filter,
  stat_filter_log10 = FALSE,
  lambda,
  omega,
  keep_outliers_mahalanobis,
  keep_outliers_isolation,
  keep_outliers_iqr,
  keep_outliers_zscore,
  keep_outliers_mad,
  individual_filters_A,
  individual_filters_B,
  individual_filters_C,
  custom_mdthresh,
  mdthresh_mode,
  mahalanobis_reference,
  selected_columns,
  include_plot_notes,
  use_manual_point_size,
  manual_point_size,
  selected_groups,
  is_categorical_group
)
}
\arguments{
\item{xlsx_file}{Path to the uploaded \code{.xlsx} file (temp upload path).}

\item{working_dir}{Directory to resolve relative paths against; the
caller must already have \code{setwd()}'d into it.}

\item{output_dir}{Base output directory for a real (non-preview) save.}

\item{element_A, element_B, element_C}{Ternary-axis element specs, each a
\verb{list(col = <one or more column names>)}.}

\item{optional_param1}{Optional \verb{list(col = <column name(s)>, filter = <filter string or NULL>)} for point size/type representation.}

\item{optional_param2}{Optional \verb{list(col = <column name(s)>, filter = <filter string or NULL>)} for point color / categorical grouping.}

\item{color_palette}{Palette name (\code{"blue"}, \code{"red"}, \code{"viridis"}, or
\code{"rainbow"}) used when \code{optional_param2} drives point color.}

\item{xlsx_display_name}{Optional original filename, preferred over
\code{xlsx_file}'s temp-upload basename for titles/output filenames.}

\item{preview}{If \code{TRUE}, skip directory/file creation - this call is
only feeding a live preview render, not a save.}

\item{use_mahalanobis}{Apply Mahalanobis-distance outlier filtering.}

\item{reference_data}{Optional reference dataset for multivariate
filtering, when \code{mahalanobis_reference}/an isolation-forest reference
mode needs one other than the file being processed. Expected as the
raw/uncurated dataset (e.g. \code{rv$df1}/\code{rv$df2}, straight off disk) -
this function curates it internally (same element/optional-parameter
and statistical filters as \code{M}, same parameters) before it reaches
\code{\link[=apply_multivariate_filtering]{apply_multivariate_filtering()}}, falling back to the raw dataset if
curation errors (e.g. a missing column). Ignored/unused when
\code{mahalanobis_reference == "self"}.}

\item{optional_param1_representation}{\code{"point_size"} or \code{"point_type"} -
how \code{optional_param1} maps onto the plotted points.}

\item{output_format}{File format for a real save (e.g. \code{"png"}).}

\item{use_isolation_forest}{Apply isolation-forest outlier filtering.}

\item{isolation_ntrees, isolation_contamination}{Number of trees and
contamination fraction for \code{\link[=compute_isolation_forest]{compute_isolation_forest()}}, when
\code{use_isolation_forest = TRUE}. User-adjustable in the UI; default 200
trees, 0.10 contamination.}

\item{isolation_sample_size}{Rows each isolation tree trains on. \code{NULL}
(default) uses every complete reference row; a whole number \verb{>= 2}
sub-samples that many per tree (see \code{\link[=compute_isolation_forest]{compute_isolation_forest()}}).}

\item{use_iqr_filter, use_zscore_filter, use_mad_filter}{Apply IQR /
Z-score / MAD statistical outlier filtering. Only one
statistical/multivariate filter is meant to be active per plot -
enforced upstream in \code{\link[=general_ternary_plot]{general_ternary_plot()}}.}

\item{stat_filter_log10}{If \code{TRUE}, the active IQR/Z-score/MAD fence is
fitted on \code{log10(value)} rather than the raw value (passed straight
through to \code{\link[=apply_statistical_filtering]{apply_statistical_filtering()}}). Default \code{FALSE}.}

\item{lambda, omega}{Sensitivity/leniency parameters for the automatic
Mahalanobis threshold formula (see \code{\link[=compute_mahalanobis_distance]{compute_mahalanobis_distance()}}).}

\item{keep_outliers_mahalanobis, keep_outliers_isolation, keep_outliers_iqr, keep_outliers_zscore, keep_outliers_mad}{If \code{TRUE} for the active filter method, keep only the flagged
outliers instead of removing them.}

\item{individual_filters_A, individual_filters_B, individual_filters_C}{Named lists (by column) of per-element filter strings, as built by
\code{collect_main_ternary_filters()}.}

\item{custom_mdthresh}{Manual Mahalanobis distance threshold, used when
\code{mdthresh_mode == "manual"}.}

\item{mdthresh_mode}{\code{"auto"} or \code{"manual"} Mahalanobis threshold mode.}

\item{mahalanobis_reference}{Which dataset the Mahalanobis/isolation-
forest reference distribution is fit to (\code{"self"}, \code{"dataset1"}, or
\code{"dataset2"}).}

\item{selected_columns}{Character vector of numeric columns used for
multivariate/statistical filtering (independent of the ternary axes).}

\item{include_plot_notes}{If \code{TRUE}, include the filter/method summary
notes text alongside the plot.}

\item{use_manual_point_size}{If \code{TRUE}, use \code{manual_point_size} for
every point instead of \code{optional_param1_representation}'s mapping.}

\item{manual_point_size}{Fixed point size used when
\code{use_manual_point_size} is \code{TRUE}.}

\item{selected_groups}{Character vector of categorical group values
(from \code{optional_param2}) to include, when \code{is_categorical_group} is
\code{TRUE}; other rows are excluded.}

\item{is_categorical_group}{Whether \code{optional_param2}'s column has been
detected as categorical (drives group-based filtering/coloring
instead of a continuous color scale).}
}
\value{
This function's entire local environment as a list
(\code{as.list(environment())}), deliberately untyped: dozens of locals
computed here (\code{clean_labels_A}/\code{B}/\code{C}, \code{ternary_points1},
\code{pointSize}/\code{pointCol}/\code{pointType}, \code{plot_title}, \code{title_parts},
\code{param1_values}, \code{param1_bins}, \code{unique_groups}, \code{group_colors},
\code{group_counts}, \code{col1_text}/\code{col2_text}/\code{col3_text}, \code{mahal_result},
\code{iso_result}, and more) are all read by the preview and/or save
renderers via \code{pd$name} or \code{with(pd, {...})}, and hand-picking a
narrower return list risked silently dropping one.
}
\description{
Does everything \code{\link[=general_ternary_plot]{general_ternary_plot()}} needs exactly once, regardless
of preview vs. save: loads the Excel file, applies individual-element,
optional-parameter, statistical, and multivariate filters, computes
ternary coordinates, builds the plot title, and works out point
size/color/shape. Touches no graphics device - see
\code{ternary_plot_preview.R}/\code{ternary_plot_save.R}'s renderers for that.
The caller is responsible
for \code{setwd(working_dir)} (with \code{on.exit()} restore) before calling this,
since that needs to stay in effect across this call and the render call
that follows it.
}
\section{Restructuring}{

This function's logic is split into seven top-level, independently
testable/documented functions, called below in this order and merged
back into this function's own local environment via \code{list2env()}:
\code{\link[=load_and_validate_ternary_source_data]{load_and_validate_ternary_source_data()}}, \code{\link[=apply_element_and_parameter_filters]{apply_element_and_parameter_filters()}}
(moves three of this function's five local closures into its own scope,
leaving \code{preview_title_layout()}/\code{calculate_plot_dimensions()} as the
two that still live here), \code{\link[=apply_statistical_filtering]{apply_statistical_filtering()}},
\code{\link[=apply_multivariate_filtering]{apply_multivariate_filtering()}}, \code{\link[=resolve_ternary_output_directory]{resolve_ternary_output_directory()}},
\code{\link[=compute_ternary_coordinates]{compute_ternary_coordinates()}}, \code{\link[=build_ternary_plot_title]{build_ternary_plot_title()}}, and
\code{\link[=compute_point_styling]{compute_point_styling()}}.
}

