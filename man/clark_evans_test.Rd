% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_clustering_analysis.R
\name{clark_evans_test}
\alias{clark_evans_test}
\title{Clark-Evans nearest-neighbour test of complete spatial randomness}
\usage{
clark_evans_test(
  x,
  y,
  n_sim = NULL,
  seed = 42,
  nn_method = c("kdtree", "matrix")
)
}
\arguments{
\item{x}{Numeric vector of X coordinates.}

\item{y}{Numeric vector of Y coordinates. Requires at least 3 valid
(finite) points, spanning a non-zero area.}

\item{n_sim}{Number of Monte Carlo simulations. Defaults to 999 for
\code{nn_method = "kdtree"}, or 299/199/99 (depending on \code{n}) for
\code{nn_method = "matrix"}, to keep runtime reasonable either way.}

\item{seed}{RNG seed for the Monte Carlo simulation; the caller's RNG
state is saved and restored afterward. Default 42.}

\item{nn_method}{Nearest-neighbour algorithm, passed to
\code{compute_nearest_neighbor_distances()}: \code{"kdtree"} (default, fast,
exact) or \code{"matrix"} (slow for large \code{n}, exact). Both give identical
results - this only affects runtime.}
}
\value{
A list: \code{n}, \code{area}, \code{density}, \code{nnd} (per-point distances),
\code{Dobs}, \code{Dpois}, \code{Dkevin}, \code{R}, \code{SE_R}, \code{Z}, \code{p_value_asymptotic},
\code{n_sim}, \code{nn_method}, \code{p_value_monte_carlo}, \code{p_value} (alias for the
Monte Carlo one), \code{verdict} (text summary).
}
\description{
Reports both a Donnelly (1978) edge-corrected asymptotic Z-test and a
Monte Carlo p-value (simulating \code{n_sim} CSR point sets in the same
bounding box). Both are known to over-reject somewhat at small n - see
this file's header comment and the package's Statistical Appendix
vignette for the empirically-measured false-positive rates and why the
bias is asymmetric (favors false "dispersed" verdicts, not "clustered" ones).
}
