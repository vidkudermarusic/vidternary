% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternary_plot_data_prep.R
\name{draw_plot_notes_column}
\alias{draw_plot_notes_column}
\title{Draw one plot-notes column as a stack of individually-positioned mtext() lines}
\usage{
draw_plot_notes_column(text, side, start_line, cex, col, adj, outer = TRUE)
}
\arguments{
\item{text}{A \code{"\\n"}-joined block of lines (e.g. \code{col1_text}/\code{col2_text}/
\code{col3_text} from \code{prepare_ternary_plot_data()}). Empty lines are
skipped.}

\item{side}{Passed to \code{mtext()} - which plot margin to draw in.}

\item{start_line}{Where the FIRST line is drawn (\code{mtext()}'s \verb{line=} for
that line); each subsequent line is drawn further out by \code{cex * 1.1}.}

\item{cex}{Text size, passed to \code{mtext()}.}

\item{col}{Text color, passed to \code{mtext()}.}

\item{adj}{Horizontal justification, passed to \code{mtext()} (\code{0} = left,
\code{0.5} = center, \code{1} = right).}

\item{outer}{Passed to \code{mtext()}. Default \code{TRUE} (this app always draws
plot notes in the outer margin).}
}
\value{
\code{invisible(NULL)}. Called for its plotting side effect on the
current graphics device.
}
\description{
Draw one plot-notes column as a stack of individually-positioned mtext() lines
}
