% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate.R
\name{validate_multivariate_data}
\alias{validate_multivariate_data}
\title{Validate and clean two datasets for multivariate analysis}
\usage{
validate_multivariate_data(
  data1,
  data2,
  selected_columns,
  method = "Multivariate Analysis",
  min_obs_ratio = 2
)
}
\arguments{
\item{data1}{First dataset, as a data frame.}

\item{data2}{Second (typically reference) dataset, as a data frame.}

\item{selected_columns}{Character vector of numeric column names to
use, mandatory (length >= 2).}

\item{method}{Method name used only in the sample-size error message.
Default \code{"Multivariate Analysis"}.}

\item{min_obs_ratio}{Minimum ratio of complete observations to
variables required in each dataset. Default 2.}
}
\value{
A list: \code{data1_clean}, \code{data2_clean} (complete-case data
frames), \code{common_cols}, \code{n_vars}, \code{n_obs1}, \code{n_obs2},
\code{condition_number}, \code{high_correlations}, \code{zero_var_cols}.
}
\description{
Checks that \code{selected_columns} exist, are numeric, and number at least
2, in both datasets; drops incomplete rows; checks each dataset has
enough observations relative to the number of variables
(\code{min_obs_ratio}); and warns (without erroring) on zero-variance
columns, high pairwise correlations, or a poorly-conditioned covariance
matrix in \code{data2}.
}
