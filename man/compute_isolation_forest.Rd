% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate.R
\name{compute_isolation_forest}
\alias{compute_isolation_forest}
\title{Flag outliers in one dataset using an Isolation Forest trained on another}
\usage{
compute_isolation_forest(
  data1,
  data2,
  selected_columns,
  contamination = 0.1,
  keep_outliers = FALSE,
  ntrees = 200,
  score_type = "score",
  seed = 42,
  sample_size = NULL
)
}
\arguments{
\item{data1}{Dataset to score for outliers, as a data frame.}

\item{data2}{Reference dataset the model is trained on, as a data frame.}

\item{selected_columns}{Character vector of column names to use for analysis.}

\item{contamination}{Expected proportion of outliers (0-1). Default 0.10.
User-adjustable in the UI (Ternary Plots' Isolation Forest panel,
Data Comparison's Isolation Forest/Comprehensive panels) - both surface
whatever value was actually used in their results text, since a
reviewer reading the app's own output should be able to see it without
reading source.}

\item{keep_outliers}{If \code{TRUE}, \code{kept_mask}/\code{filtered_data1} keep the
flagged outliers instead of the inliers. Default \code{FALSE}.}

\item{ntrees}{Number of trees in the isolation forest. Default 200.
User-adjustable in the UI, same as \code{contamination} above.}

\item{score_type}{Score type passed to \code{predict.isolation_forest()}, \code{"score"} or \code{"outlier"}. Default \code{"score"}.}

\item{seed}{Random seed for reproducibility, passed through directly to
\code{isotree::isolation.forest()}'s own \code{seed} argument (which governs its
randomness independently of R's \code{set.seed()} - confirmed empirically,
not assumed). Default 42. User-adjustable in the UI, same as
\code{contamination}/\code{ntrees} above - change it to sensitivity-check
whether outlier calls are stable across different random forest
realizations.}

\item{sample_size}{Number of reference rows each tree is trained on.
\code{NULL} (default) uses every complete reference row - simple and fully
reproducible, but a departure from the algorithm as published: Liu,
Ting & Zhou (2008) sub-sample (their default \code{psi = 256}) specifically
so trees stay shallow and the anomaly-score normalisation \code{c(psi)}
matches the sub-sample. Pass a whole number \verb{>= 2} to sub-sample that
many rows per tree instead; a value above the number of complete
reference rows is clamped down to it. The value actually used is
reported back in \code{sample_size}.}
}
\value{
A list: \code{model} (the fitted isolation forest), \code{columns_used},
\code{threshold}, \code{contamination}, \code{ntrees}, \code{seed}, \code{sample_size} (the row count
each tree was actually trained on - either every complete reference
row, or the sub-sample size requested, clamped to the rows available),
\code{scores} (length \code{nrow(data1)}), \code{outlier_indices},
\code{kept_mask}, \code{filtered_data1}, \code{ref_scores_sum}.
}
\description{
Trains an isolation forest (\code{isotree::isolation.forest()}) on \code{data2}
(the reference), derives an anomaly-score threshold from \code{data2}'s own
scores at the \code{1 - contamination} quantile, then scores \code{data1} against
that threshold. Liu, Ting & Zhou (2008), \url{https://doi.org/10.1109/ICDM.2008.17}.
}
