% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_point_pattern_analysis.R
\name{compute_l_function}
\alias{compute_l_function}
\title{L-function (variance-stabilizing transform of Ripley's K)}
\usage{
compute_l_function(pp)
}
\arguments{
\item{pp}{A \code{spatstat.geom::ppp}, e.g. from \code{build_point_pattern()}.}
}
\value{
A data frame: \code{r}, \code{theo} (\verb{= r} under CSR), \code{obs} (edge-corrected
observed L(r)), \code{obs_minus_r} (\code{obs - r}, the conventional clustering
diagnostic - positive means more neighbours than CSR predicts at that
distance), \code{correction}.
}
\description{
\code{L(r) = sqrt(K(r) / pi)}, so complete spatial randomness plots as a
straight line \code{L(r) = r}. Computed directly via
\code{spatstat.explore::Lest()} (not derived by hand from \code{compute_ripley_k()})
so the same edge correction is applied consistently within the transform,
matching spatstat's own recommended usage.
}
