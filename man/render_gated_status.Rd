% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{render_gated_status}
\alias{render_gated_status}
\title{Render a gated status message from inside a \code{renderText()} block}
\usage{
render_gated_status(input, button_id, placeholder_msg, build_message_fn)
}
\arguments{
\item{input}{The Shiny \code{input} object.}

\item{button_id}{Character; id of the gating \code{actionButton}.}

\item{placeholder_msg}{Message to show before the button is clicked, or on an empty-message error.}

\item{build_message_fn}{Zero-arg function that computes and returns the real status message string.}
}
\value{
Character status message.
}
\description{
Returns \code{placeholder_msg} until \code{input[[button_id]]} has been clicked;
after that, calls \code{build_message_fn()} and returns its result, mapping an
empty-message \code{shiny.silent.error} (from \code{req()}/\code{shiny::validate()}) back
to \code{placeholder_msg} while re-throwing any other error so Shiny's own
output machinery can display it.
}
