% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_pre_analysis_filter.R
\name{create_pre_filter_ui}
\alias{create_pre_filter_ui}
\title{Build the static part of a tab's "Pre-Analysis Data Filter" UI section}
\usage{
create_pre_filter_ui(ns, prefix)
}
\arguments{
\item{ns}{The tab's own namespacing function (\code{NS(id)}, as already built
by every \verb{create_*_tab()}).}

\item{prefix}{Short, tab-unique input-ID prefix (e.g. \code{"evs"},
\code{"spatial"}, \code{"ppp"}) - keeps this section's input IDs from colliding
with the tab's own other inputs, and lets three separate tabs each
safely use these same functions inside their own module namespace.}
}
\value{
A \code{shiny::div()}.
}
\description{
A column selector plus a \code{uiOutput()} placeholder for the dynamic
per-column filter boxes, rendered separately by
\code{render_pre_filter_inputs()} (called from the tab's own \code{renderUI()}, so
the number of filter boxes can grow/shrink as columns are selected).
}
